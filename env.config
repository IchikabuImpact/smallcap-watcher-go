# Runtime config for Node.js on the VPS host.
DB_HOST=localhost:3306
DB_USER=jpx_user
DB_PASSWORD=jpx_password
DB_NAME=jpx_data

# Base URL for the scraper API (no trailing slash needed).
SCRAPER_BASE_URL=http://localhost:8085
SCRAPER_REQUEST_INTERVAL=3s

# Static site output directory served by Apache.
OUTPUT_DIR=public
INDEX_MAX_AGE=36h
